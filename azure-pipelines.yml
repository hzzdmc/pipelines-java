# When manually running the pipeline, you can select whether it
# succeeds or fails.
parameters:
- name: succeed
  displayName: Succeed or fail
  type: boolean
  default: false

trigger: none


pool:
  vmImage: ubuntu-latest

jobs:
- job: Work33
  steps:
  - script: echo Hello, world33!
    displayName: 'Run a one-line script'



